{
  "description": "Embed chunk content with MiniLM at ingest time",
  "processors": [
    {
      "text_embedding": {
        "model_id": "REPLACE_AT_INIT",
        "field_map": {
          "content": "embedding"
        }
      }
    }
  ]
}
